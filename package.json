{
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:full": "concurrently \"cd backend && python run.py\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix frontend && cd backend && pip install -r requirements.txt && cd ..",
    "start:backend": "cd backend && python run.py",
    "start:frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "recharts": "^3.8.0",
    "concurrently": "^8.2.0"
  }
}
